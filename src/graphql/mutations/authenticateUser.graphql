mutation authenticateUser($facebookToken: String!) {
  authenticateUser(facebookToken: $facebookToken) {
    token
    refreshToken
    user {
      id
      fullName
      firstName
      lastName
      username
      avatarUrl
    }
  }
}
