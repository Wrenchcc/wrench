#import "../fragments/UserSettingsFragment.graphql"

mutation toggleNotificationSettings($input: ToggleNotificationSettingsInput) {
  toggleNotificationSettings(input: $input) {
    ...UserSettings
  }
}