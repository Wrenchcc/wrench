# Upload lambda@edge to AWS

The scripts in this directory solve both of these problems. To get started:

- Make sure you have run yarn: `yarn`
- Add `AWS_ACCESS_KEY`, `AWS_SECRET_ACCESS_KEY`.

To deploy, run:
`yarn deploy`
