// import { DateTime } from 'luxon'

// export default async (_, { type, startDate = DateTime.local().minus({ month: 1 }), endDate }) => {
//   console.log(type, startDate, endDate)

//   return [{ data: new Date(), count: 100 }]
// }
