 
web:
  component: ./infra/serverless-next/serverless.js
  inputs:
    domain: "wrench.cc"
    bucketName: wrench-web-edge
    build: true
    lambda:
      name: wrench-web #todo
      memory: 2048
    cloudfront:
      compress: true
      forward:
        headers: ['CloudFront-Viewer-Country', 'Accept-Language']
