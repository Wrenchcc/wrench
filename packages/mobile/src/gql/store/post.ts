import { makeVar } from '@apollo/client'

export const isPostingVar = makeVar(false)
export const captionVar = makeVar('')
export const collectionVar = makeVar('')

export const videoIdInViewport = makeVar('')
